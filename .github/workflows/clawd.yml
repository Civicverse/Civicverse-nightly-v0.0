name: ClawdBot Master Builder

on:
  workflow_dispatch:

jobs:
  sanity-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Sanity output
        run: |
          echo "ClawdBot is running"
          ls -la
